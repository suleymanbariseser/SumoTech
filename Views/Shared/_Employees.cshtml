@using SumoTech.Entity
@model IEnumerable<Employee>

<h3 class="card-header text-center font-weight-bold text-uppercase py-4">Employees</h3>
<div class="card-body">
    <div id="table" class="table-editable">
        <span class="table-add float-right mb-3 mr-2"><a href="/Employee/Create" class="text-success"><i class="fas fa-plus fa-2x" aria-hidden="true"></i></a></span>
        <table class="table table-bordered table-responsive-md table-striped text-center">
            <thead>
                <tr>
                    <th class="text-center">First Name</th>
                    <th class="text-center">Last Name</th>
                    <th class="text-center">Phone</th>
                    <th class="text-center">Salary</th>
                    <th class="text-center">Hire Date</th>
                    <th class="text-center">Edit</th>
                    <th class="text-center">Remove</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var employee in Model)
                {
                    <tr>
                        <td class="pt-3-half">@employee.FirstName</td>
                        <td class="pt-3-half">@employee.LastName</td>
                        <td class="pt-3-half"><a href="tel:@employee.Phone">@employee.Phone</a></td>
                        <td class="pt-3-half">@employee.Salary</td>
                        <td class="pt-3-half">@employee.HireTime</td>
                        <td class="pt-3-half">
                            <a class="edit-link" href="/Employee/Edit/@employee.Id"><i class="fas fa-edit"></i></a>
                        </td>
                        <td>
                            <span class="table-remove"><button type="button" class="btn btn-danger btn-rounded btn-sm my-0"><a href="/Employee/Delete/@employee.Id">Remove</a></button></span>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>